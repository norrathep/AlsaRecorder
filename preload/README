Compile and insert preload:

gcc -g -fPIC -shared -Wl,-soname,preload.so -ldl -o preload.so  main.c
export LD_PRELOAD=./preload.so


To convert out.raw produced by cartoon001.wav, do the following:
change mult var in main.c to 2
sox -r 11025 -b 16 -e signed-integer -c 2 out.raw out.wav

For Spotify recording:
change mult var in main.c to 8
sox -r 44100 -b 32 -e signed-integer -c 2 out.raw out.wav

Play out.wav -> aplay out.wav
